import { ActiveCartePipe } from './active-carte.pipe';

describe('ActiveCartePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveCartePipe();
    expect(pipe).toBeTruthy();
  });
});
